/* sorting array with prperty */
homes.sort((a, b) => parseFloat(a.price) - parseFloat(b.price));
/* sorting array with prperty */

/* sorting array with prperty */
users.sort(function(a, b){
    if(a.firstname < b.firstname) { return -1; }
    if(a.firstname > b.firstname) { return 1; }
    return 0;
})
/* sorting array with prperty */